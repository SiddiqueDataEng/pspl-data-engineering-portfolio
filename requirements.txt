# Core data processing
pandas==2.2.2
pyarrow==16.1.0
fastavro==1.9.4
avro-python3==1.10.2
numpy==1.26.4

# PySpark + Delta Lake
pyspark==3.5.1
delta-spark==3.2.0

# dbt
dbt-core==1.8.4
dbt-duckdb==1.8.1
# 1.0.x delta_scan fails on Spark 3.5 / Delta 3.2 logs (MissingVersionError); 1.5+ reads them.
duckdb==1.5.2

# Jupyter
jupyter==1.0.0
jupyterlab==4.2.3
nbconvert==7.16.4

# Visualization
# 3.9.1 often resolves to sdist on Windows (needs MSVC); 3.8.4 has cp311 wheels.
matplotlib==3.8.4
plotly==5.22.0
streamlit==1.39.0

# Testing
pytest==8.2.2
hypothesis==6.108.5
pytest-spark==0.6.0

# Utilities
faker==25.9.2
python-dotenv==1.0.1

# Optional: generate trainer PowerPoint (docs/training/pspl_trainer_overview.pptx)
python-pptx==1.0.2
